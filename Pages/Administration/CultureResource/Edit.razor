@using MudBlazor
@using STZ.Shared.Entities
@using STZ.Frontend.Components
<MudForm>
    <MudGrid>
        <MudItem xs="12" sm="6">
            <MudTextField T="string" Label="Cultura" Value="@Item.Culture?.Name" ReadOnly />
        </MudItem>
        <MudItem xs="12" sm="6">
            <MudTextField T="string" Label="Recurso" Value="@Item.Resource?.Code" ReadOnly />
        </MudItem>
    </MudGrid>
    
    <MudItem xs="12" Class="mt-4">
        <MudTextField T="string" Label="Texto" Lines="4" @bind-Text="Item.Text" Variant="Variant.Outlined" />
    </MudItem>
    
    <MudStack Row Class="mt-4" Justify="Justify.FlexEnd">
        <MudButton OnClick="() => MudDialog.Cancel()"><STZText Key="General.Cancel" /></MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" 
                   OnClick="() => MudDialog.Close(DialogResult.Ok(Item))"><STZText Key="General.Update" /></MudButton>
    </MudStack>
</MudForm>

@code {
    [Parameter] public ResourceCulture<Guid, Guid> Item { get; set; } = new();
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = default!;

}