@page "/Resources"
@attribute [STZAuthorize("CultureResources")]
@using Microsoft.AspNetCore.Components.Web
@using STZ.Frontend.Components
@using STZ.Frontend.Pages.Administration
@using STZ.Shared.Entities
@using MudBlazor
@using STZ.Frontend.Authorization

<PageTitle><STZText Key="Resources.Title" /></PageTitle>

<STZDataGrid TItem="ResourceCulture<Guid, Guid>" TId="Guid" Title="Resources.Title" 
             AddComponentType="typeof(Add)"
             EditComponentType="typeof(Edit)"
             DetailComponentType="typeof(View)">
    <Columns>
        <STZColumn T="ResourceCulture<Guid, Guid>" TProperty="string" 
                        Property="x => x.Resource.Code" 
                        Title="Código"
                        Grouping="true" />
        <STZColumn T="ResourceCulture<Guid, Guid>" TProperty="string" Property="x => x.Culture.Code" Title="Cultura" Hideable="false" />
        <STZColumn T="ResourceCulture<Guid, Guid>" TProperty="string" Property="x => x.Text" Title="Texto" Hideable="false" />
    </Columns>
</STZDataGrid>